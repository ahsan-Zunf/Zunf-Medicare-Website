[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd Backend && npm install && npm start"
# Note: It's better to set "Root Directory" to "/Backend" in Railway Settings.
# This config is a fallback in case you are deploying from the root.
